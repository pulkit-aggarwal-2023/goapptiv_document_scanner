class AndroidOptions {
  final String scanTitle;
  final String cropTitle;
  final String blackAndWhiteTitle;
  final String resetTitle;

  const AndroidOptions({
    this.scanTitle = 'Scan Image',
    this.cropTitle = 'Crop Image',
    this.blackAndWhiteTitle = 'Black And White',
    this.resetTitle = 'Reset Image',
  });
}
